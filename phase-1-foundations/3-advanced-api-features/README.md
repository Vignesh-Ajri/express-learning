# Advanced API Features (Express + MongoDB)

This project covers advanced backend features used in real-world APIs.

## Features

- Filtering, Sorting, Field Limiting, Pagination
- Aggregation Pipelines (Analytics)
- Security (Helmet, Rate Limiting, XSS Protection)
- Performance Optimization (Compression, HPP)
- Versioning (v1 & v2 routes)
- Nested Routes & Population (Users ↔ Posts)
- Environment Config with dotenv
- Modular Folder Structure

## 🛠️ Tech Stack

- Node.js, Express.js
- MongoDB, Mongoose
- Helmet, express-rate-limit, hpp, compression, dotenv

## ⚙️ Setup

```bash
npm install
npm run dev
```

## Create a .env file

```bash
PORT=5000
DATABASE_URL=mongodb://localhost:27017/advanced-api
NODE_ENV=development
```
